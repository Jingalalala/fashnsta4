function formatPrice(p){return `₹${Number(p||0).toLocaleString("en-IN")}`;}
function productCard(p){return `<div class="product-card"><a href="product.html?id=${p.id}"><div class="product-media">${p.badge?`<span class="tag">${p.badge}</span>`:""}<img src="${p.image||'assets/images/placeholder.jpg'}" alt="${p.name}"></div><div class="product-info"><p class="product-title">${p.name}</p><p class="product-meta">${p.category||'Fashnsta Edit'}</p><div class="price-row"><span class="price">${formatPrice(p.price)}</span>${p.oldprice?`<span class="old-price">${formatPrice(p.oldprice)}</span>`:""}</div></div></a><div class="product-actions"><button class="btn gold full" onclick='addToCartById("${p.id}")'>Add to Cart</button></div></div>`;}
function renderProducts(products){const grid=document.getElementById("productGrid");if(!grid)return;grid.innerHTML=products.map(productCard).join("");}
